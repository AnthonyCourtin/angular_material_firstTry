# my-personal-website

This project is generated with [yo angular generator](https://github.com/yeoman/generator-angular) version 0.15.1 and using [Angular Material](https://github.com/angular/material) version 1.0.7

## Build & development

Run `grunt` for building and `grunt serve` for preview.

## Testing

Running `grunt test` will run the unit tests with karma.


